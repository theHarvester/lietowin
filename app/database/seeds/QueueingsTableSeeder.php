<?php

class QueueingsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('queueings')->truncate();

		$queueings = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('queueings')->insert($queueings);
	}

}
